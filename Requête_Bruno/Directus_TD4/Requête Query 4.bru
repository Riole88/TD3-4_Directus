meta {
  name: Requête Query 4
  type: http
  seq: 4
}

post {
  url: http://localhost:8055/graphql
  body: json
  auth: inherit
}

body:json {
  {
    "query": "query { praticien(filter: { ville: { _eq: \"Paris\" } }) { id nom prenom telephone ville specialite_id { libelle } structure_id {ville}} }"
  }
  
}

settings {
  encodeUrl: true
  timeout: 0
}
