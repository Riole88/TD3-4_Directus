meta {
  name: Requête Query 6
  type: http
  seq: 6
}

post {
  url: http://localhost:8055/graphql
  body: json
  auth: inherit
}

body:json {
  {
    "query": "query { praticien(filter: {structure_id:{ville:{_eq : \"\tParis\"}}}) { id nom prenom email telephone ville specialite_id { libelle } structure_id {ville}} }"
  }
  
}

settings {
  encodeUrl: true
  timeout: 0
}
