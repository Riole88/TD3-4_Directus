meta {
  name: Requête Query 9
  type: http
  seq: 9
}

post {
  url: http://localhost:8055/graphql
  body: json
  auth: inherit
}

body:json {
  {
    "query": "query ($ville: String!) { praticien(filter: { ville: { _eq: $ville } }) { id nom prenom telephone ville specialite_id { libelle } } }",
    "variables": {
      "ville": "Paris"
    }
  }
  
}

settings {
  encodeUrl: true
  timeout: 0
}
